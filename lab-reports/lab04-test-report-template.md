# Звіт з тестування REST API
## Labwork 2-4: API Testing Report

**Виконавець:** [Ваше ім'я]  
**Дата:** 13 грудня 2025  
**Версія API:** v1, v2 

---

## 1. Загальна інформація

### 1.1 Мета тестування
Перевірка функціональності REST API інтернет-магазину кросівок, включаючи:
- CRUD операції з товарами
- Отримання категорій
- Відправка відгуків
- Валідація даних
- Обробка помилок

### 1.2 Тестове середовище
- **Сервер:** http://localhost:5000
- **Інструмент:** Postman
- **База даних:** SQLite (db.sqlite)
- **Документація:** Swagger UI (http://localhost:5000/apidocs)

---

## 2. Endpoints для тестування

| Метод | Endpoint | Опис |
|-------|----------|------|
| GET | `/api/v1/products` | Отримати список товарів |
| GET | `/api/v1/products/<id>` | Отримати товар за ID |
| POST | `/api/v1/products` | Створити новий товар |
| DELETE | `/api/v1/products/<id>` | Видалити товар |
| GET | `/api/v1/categories` | Отримати список категорій |
| POST | `/api/v1/feedback` | Надіслати відгук |
| POST | `/api/v2/products` | Створити товар (v2, строга валідація) |

---

## 3. Тестові сценарії та результати

### 3.1 GET `/api/v1/products` - Отримати всі товари

**Тестовий запит:**
```
GET http://localhost:5000/api/v1/products
```

**Очікуваний результат:**
- Статус код: 200
- Тіло відповіді: JSON масив товарів

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.2 GET `/api/v1/products?category_id=1` - Фільтрація за категорією

**Тестовий запит:**
```
GET http://localhost:5000/api/v1/products?category_id=1
```

**Очікуваний результат:**
- Статус код: 200
- Повертає лише товари з category_id = 1

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.3 GET `/api/v1/products?search=nike` - Пошук товарів

**Тестовий запит:**
```
GET http://localhost:5000/api/v1/products?search=nike
```

**Очікуваний результат:**
- Статус код: 200
- Повертає товари, що містять "nike" в назві або описі

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.4 GET `/api/v1/products/1` - Отримати товар за ID

**Тестовий запит:**
```
GET http://localhost:5000/api/v1/products/1
```

**Очікуваний результат:**
- Статус код: 200
- Повертає об'єкт товару з id=1

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.5 GET `/api/v1/products/99999` - Неіснуючий товар

**Тестовий запит:**
```
GET http://localhost:5000/api/v1/products/99999
```

**Очікуваний результат:**
- Статус код: 404
- JSON: `{"error": "Product not found"}`

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.6 POST `/api/v1/products` - Створити товар

**Тестовий запит:**
```json
POST http://localhost:5000/api/v1/products
Content-Type: application/json

{
  "name": "Test Sneakers",
  "description": "Test product for API",
  "price": 99.99,
  "image_url": "/static/uploads/test.jpg",
  "category_id": 1
}
```

**Очікуваний результат:**
- Статус код: 201
- JSON з id створеного товару: `{"id": <number>}`

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.7 POST `/api/v1/products` - Створити без обов'язкових полів

**Тестовий запит:**
```json
POST http://localhost:5000/api/v1/products
Content-Type: application/json

{
  "description": "Missing name and price"
}
```

**Очікуваний результат:**
- Статус код: 400
- JSON: `{"error": "Missing \"name\" or \"price\""}`

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.8 DELETE `/api/v1/products/<id>` - Видалити товар

**Тестовий запит:**
```
DELETE http://localhost:5000/api/v1/products/10
```

**Очікуваний результат:**
- Статус код: 200 (якщо товар існує)
- JSON: `{"status": "deleted"}`

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.9 GET `/api/v1/categories` - Отримати категорії

**Тестовий запит:**
```
GET http://localhost:5000/api/v1/categories
```

**Очікуваний результат:**
- Статус код: 200
- JSON масив категорій

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.10 POST `/api/v1/feedback` - Надіслати відгук

**Тестовий запит:**
```json
POST http://localhost:5000/api/v1/feedback
Content-Type: application/json

{
  "user_name": "Тестовий користувач",
  "email": "test@example.com",
  "message": "Чудовий магазин!"
}
```

**Очікуваний результат:**
- Статус код: 201
- JSON: `{"status": "ok"}`

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.11 POST `/api/v2/products` - Створити товар (API v2)

**Тестовий запит:**
```json
POST http://localhost:5000/api/v2/products
Content-Type: application/json

{
  "name": "Puma RS-X v2",
  "description": "Created via API v2",
  "price": 125.50,
  "image_url": "/static/uploads/puma-v2.jpg",
  "category_id": 2
}
```

**Очікуваний результат:**
- Статус код: 201
- JSON з повним об'єктом створеного товару

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

### 3.12 POST `/api/v2/products` - Валідація v2 (некоректні дані)

**Тестовий запит:**
```json
POST http://localhost:5000/api/v2/products
Content-Type: application/json

{
  "name": "",
  "price": "not_a_number"
}
```

**Очікуваний результат:**
- Статус код: 400
- JSON з описом помилки валідації

**Фактичний результат:**
- [ ] ✅ Пройдено
- [ ] ❌ Не пройдено

**Коментарі:**
```
[Опишіть результат тесту]
```

---

## 4. Зведена таблиця результатів

| № | Endpoint | Метод | Статус | Примітки |
|---|----------|-------|--------|----------|
| 1 | `/api/v1/products` | GET | ⬜ | |
| 2 | `/api/v1/products?category_id=1` | GET | ⬜ | |
| 3 | `/api/v1/products?search=nike` | GET | ⬜ | |
| 4 | `/api/v1/products/1` | GET | ⬜ | |
| 5 | `/api/v1/products/99999` | GET | ⬜ | 404 error |
| 6 | `/api/v1/products` | POST | ⬜ | Create |
| 7 | `/api/v1/products` (invalid) | POST | ⬜ | 400 error |
| 8 | `/api/v1/products/<id>` | DELETE | ⬜ | |
| 9 | `/api/v1/categories` | GET | ⬜ | |
| 10 | `/api/v1/feedback` | POST | ⬜ | |
| 11 | `/api/v2/products` | POST | ⬜ | v2 API |
| 12 | `/api/v2/products` (invalid) | POST | ⬜ | v2 validation |

**Легенда:** ✅ - Успішно | ❌ - Помилка | ⬜ - Не виконано

---

## 5. Виявлені проблеми

### 5.1 Критичні проблеми
```
[Опишіть критичні проблеми, якщо є]
```

### 5.2 Некритичні проблеми
```
[Опишіть некритичні проблеми, якщо є]
```

### 5.3 Пропозиції щодо покращення
```
[Ваші пропозиції]
```

---

## 6. Додаткове тестування

### 6.1 Продуктивність
- Час відповіді для GET запитів: ___ мс
- Час відповіді для POST запитів: ___ мс

### 6.2 Документація Swagger
- [ ] Swagger UI доступний за адресою `/apidocs`
- [ ] Всі endpoints задокументовані
- [ ] Приклади запитів/відповідей присутні
- [ ] Можна виконувати запити з UI

---

## 7. Висновки

### 7.1 Загальна оцінка
```
[Опишіть загальний стан API]
```

### 7.2 Відповідність вимогам

**Рівень 1:**
- [ ] Мінімум 6 endpoints ✅
- [ ] JSON формат даних ✅
- [ ] Базова обробка помилок ✅

**Рівень 2:**
- [ ] Документація Flasgger ✅
- [ ] Опис всіх endpoints ✅
- [ ] Схеми запитів/відповідей ✅

**Рівень 3:**
- [ ] Валідація вхідних даних ✅
- [ ] Версіонування API (v1, v2) ✅
- [ ] Розширена обробка помилок ✅

### 7.3 Рекомендації
```
[Ваші рекомендації щодо впровадження в production]
```

---

## 8. Додатки

### 8.1 Скріншоти
- [ ] Swagger UI
- [ ] Postman колекція
- [ ] Успішні запити
- [ ] Помилки (404, 400)

### 8.2 Postman Collection
Файл: `Labwork_API.postman_collection.json`

---

**Підпис тестувальника:** _________________  
**Дата завершення:** _________________
